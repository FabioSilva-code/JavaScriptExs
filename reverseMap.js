//Cria uma função que mapeia cada elemento de um array de strings para a string invertida.
//A inversão de cada string deve ser implementada utilizando uma função recursiva.

/* function mapeia(originalArray) {
    const reverseArray = originalArray.map((ele) => ele.split('').reverse().join(''));
    
    return reverseArray;
}
const originalArray = ["hello", "world", "functional", "programming"];


console.log(mapeia(originalArray)) */


function inverteArrays(){

}


function inverteStrings(array){
    //para cada elemento faz .split('').reverse().join(''));

    /* function fatorial(n) {
        if (n === 1) return 1;
        return n * fatorial(n - 1);
      } */
      // 
      let arrayzinho=[]
      let inicial=0
        return arrayzinho.unshift(array[inicial])
        inverteStrings(inicial+1)
}
let array = ["fabio","joao","wqerr"]
console.log(inverteStrings(array))
